import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.FoodUi.app',
  appName: 'foodUI',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
